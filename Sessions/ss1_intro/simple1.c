/*
 * Phát biểu tiền xử lý.
 */
#include <stdio.h>                      // Thư viện stdio.h được sử dụng để cung cấp hàm printf

/*
 * Hàm main là thành phần chính của mọi chương trình C.
 * Một chương trình C muốn chạy được cần phải có hàm main.
 */
int main() {
    printf("Hello, Simple 1!\n");       // Chỉ thị cho máy in ra chuỗi ký tự nằm trong nháy kép
    return 0;
}